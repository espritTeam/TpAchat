  version: "3"

  services:
    mysqldb:
      container_name: mysqldb1
      image: mysql:8
      restart: unless-stopped
      environment:
        MYSQL_ROOT_PASSWORD: root
  #    volumes:
   #     - /home/mysql/data:/var/lib/mysql

    spring_app:
      image: abdelwehed/imageachat
      container_name: achatproject1
      restart: on-failure
      ports:
        - 9000:9000
      depends_on:
        - mysqldb
